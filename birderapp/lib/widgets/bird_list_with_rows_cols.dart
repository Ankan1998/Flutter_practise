import 'package:flutter/material.dart';

class BirdListWithRowsCols extends StatelessWidget {
  const BirdListWithRowsCols({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Birdlist Widget');
  }
}